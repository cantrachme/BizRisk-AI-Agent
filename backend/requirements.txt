fastapi
uvicorn[standard]
langgraph
pydantic
pytest
pydantic-settings
SQLAlchemy
psycopg[binary]
alembic

playwright

# Real LLM provider (only imported when LLM_PROVIDER=anthropic; the mock/test
# path never requires this package).
anthropic
